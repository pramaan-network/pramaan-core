// Package types holds the x/docreg module's hand-written Go types (as
// opposed to the generated *.pb.go files in this same package).
//
// This particular file is intentionally empty — it's a placeholder left
// over from the ignite scaffold's default file layout (`types/types.go` is
// where scaffold-generated custom type helpers would normally go). No
// docreg-specific helper types have been added here; the module's real
// types (Document, StringList, etc.) live in types.pb.go.
package types
