// Package types holds the x/issuer module's hand-written Go types (as
// opposed to the generated *.pb.go files in this same package).
//
// This particular file is intentionally empty — a placeholder left over
// from the ignite scaffold's default file layout. The module's real types
// (Issuer, etc.) live in types.pb.go.
package types
